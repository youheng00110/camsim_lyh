# 忽略克隆的 OpenDWM 仓库（整目录忽略）


# 忽略 waymo_link 符号链接（无论指向文件还是目录）
/waymo_link
#忽略pkl文件
*.pkl          
!utils/valid.pkl  
# 只忽略特定目录下的大 json 文件（示例，根据你的实际路径改）
waymo_balanced/*.json
waymo_caption_v2/*.json
nuscenes_train_balanced/*.json
nuscenes_val_sort/*.json
waymo_prepo/*.json
avrgo2_json/*.json
nuplan-devkit-master/
mini_sensors/
# 忽略整个 output 目录（所有内容都忽略）
# 忽略 output 下所有 debug_* 目录里的 checkpoints、optimizer
output/debug_*/checkpoints/
output/debug_*/optimizer/

# 忽略 log/ 目录（如果确实要忽略的话）
output/debug_*/log/

# 强制保留 preview/ 目录（可选，防止被上层规则误杀）
!output/debug_*/preview/
!output/debug_*/preview/**